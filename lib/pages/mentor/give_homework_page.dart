import 'package:flutter/material.dart';

class GiveHomeworkPage extends StatelessWidget {
  const GiveHomeworkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(), // Tamamen boş bir sayfa gövdesi
    );
  }
}