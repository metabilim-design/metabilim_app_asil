import 'package:flutter/material.dart';

class istatistiklerPage extends StatelessWidget {
  const istatistiklerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(), // Tamamen boş bir sayfa gövdesi
    );
  }
}