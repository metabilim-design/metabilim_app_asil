import 'package:flutter/material.dart';

class CheckHomeworkPage extends StatelessWidget {
  const CheckHomeworkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(), // Tamamen boş bir sayfa gövdesi
    );
  }
}